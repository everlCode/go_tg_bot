services:
  bot:
    build: .
    container_name: gobot
    dns:
      - 8.8.8.8
      - 1.1.1.1
    ports:
      - "80:80"
    volumes:
      - ./app/db:/app/db
      - ./app/static:/app/static
    env_file:
      - .env

